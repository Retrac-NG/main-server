import { Router } from 'express';

const AssetRoutes = Router();

// prettier-ignore
AssetRoutes
    .route('/assets')

// prettier-ignore
AssetRoutes
    .route('asset/car')

// prettier-ignore
AssetRoutes
    .route('asset/bike')

// prettier-ignore
AssetRoutes
    .route('asset/phone')

// prettier-ignore
AssetRoutes
    .route('asset/laptopn')
export default AssetRoutes;
